import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Semana10_Actividad',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
